# 引き継ぎ（技術者向け）

このフォルダの中身を、後から直す人のための説明。

---

## 1. 動かすのに要るもの

| | 必要なもの |
|---|---|
| ビューアーを見る | **Python 3.9以上だけ**（標準ライブラリのみ） |
| オルソを取り込む | 上記 ＋ `pillow` `numpy`（`scipy` があれば少し速い） |
| 森林データを作り直す | Python だけ ＋ インターネット |

```
pip install pillow numpy
```

GDAL / QGIS / pyproj / pyshp / openpyxl / geopandas は**一切不要**。

---

## 2. ファイル構成

```
ナラ枯れビューアー/
  はじめにお読みください.txt
  起動.bat                        ビューアー起動
  オルソを取り込む.bat            オルソ→タイル＋候補木
  初期設定（最初に一度だけ）.bat  森林データ＋背景地図の取得
  server.py                       HTTPサーバー＋JSON API（標準ライブラリのみ）

  app/                            画面
    index.html  style.css  app.js
    vendor/leaflet.js  leaflet.css  images/     Leaflet 1.9.4（同梱・オフライン動作）

  tools/
    geo.py               平面直角座標系XI系 ⇄ 緯度経度 ⇄ Webメルカトル
    shapelib.py          shapefile / dbf / xlsx リーダ（標準ライブラリのみ）
    dem.py               国土地理院 標高タイルの取得とキャッシュ
    ortho.py             オルソの読み込みとXYZタイル化
    detect.py            ナラ枯れ候補の抽出と優先度判定
    db.py                survey.db のスキーマ定義
    build_forest.py      北海道オープンデータ → forest.db
    ingest_ortho.py      オルソ取り込みの本体
    fetch_basemap.py     背景地図のローカル取り込み
    seed_public_records.py  公表されている被害木の登録
    remove_site.py       サイトの削除

  data/
    survey.db            ★人が入力したデータ。消すな。バックアップ対象
    photos/              ★現地写真。バックアップ対象
    forest.db            林班・小班・森林調査簿（再生成可）
    layers/              林班界・ナラ類小班のGeoJSON（再生成可）
    tiles/<サイト>/      オルソのXYZタイル（再生成可）
    basemap/             国土地理院タイルの控え（再取得可）
    dem_cache/           標高タイルの控え（再取得可）
    sites.json           サイト一覧（再生成可）
    _src/                ダウンロードした元zipと展開物（消してよい）

  docs/
    運用マニュアル.md    使う人向け
    背景と設計.md        なぜこの作りにしたか
    引き継ぎ.md          これ
```

---

## 3. データの流れ

```
北海道オープンデータ 01oshima.zip
  ├ 01_渡島林班.shp / 01_渡島小班.shp   （EPSG:2459）
  └ 01_渡島_調査簿データ.xlsx           （樹種・林齢・蓄積・樹高・代表地番）
        │
        │ tools/build_forest.py
        │   市町村コード 15（森町）で抽出
        │   KEYCODE = 振興局(2)+市町村(2)+図郭(2)+林班(4)+小班(4) で調査簿と結合
        │   国土地理院DEM10Bで小班重心の標高を取得
        ▼
   data/forest.db  ── 林班165件 / 小班10,301件


ドローンオルソ (EPSG:6679)
        │ tools/ingest_ortho.py
        ├──► tools/ortho.py     → data/tiles/<サイト>/{z}/{x}/{y}.webp
        └──► tools/detect.py    → 候補領域（重心・面積・スコア）
                  │
                  │ forest.db で点in小班 → 林班・小班・樹種・地番
                  │ DEM10B          → 標高
                  │ DSM（あれば）   → 樹高の参考値
                  ▼
             data/survey.db  trees テーブル
```

---

## 4. データベース

### data/survey.db（人が入力したもの・消すな）

| テーブル | 内容 |
|---|---|
| `sites` | オルソ1枚ぶんの範囲・ズーム・タイル形式 |
| `trees` | 候補木。検出結果と現地調査結果の両方 |
| `photos` | 現地写真（実体は `data/photos/`） |
| `history` | 誰がいつ何を変えたか |

`trees` の列は2種類に分かれる。

- **検出由来**（取り込み直すと更新される）
  `lon, lat, x, y, elev, canopy_h, rinpan, kosyoban, chiku, sp_main, nara_rank, chiban, area_m2, score, priority`
- **人が入力**（取り込み直しても**絶対に触らない**）
  `status, survey_date, surveyor, species, dbh_cm, leaf_color, dieback, boring, frass, stand, misjudge_reason, access_note, treatment, treatment_date, memo`

この境界は `tools/db.py` の `SURVEY_FIELDS` と、
`server.py` の `EDITABLE` で定義している。列を足すときは両方に足すこと。

ステータスの内部コードは
`unsurveyed / damaged / clean / pending / unreachable / treated`。
表示名と色は `tools/db.py` の `STATUS` にまとめてある。ここを直せば画面も凡例も追随する。

### data/forest.db（再生成可）

| テーブル | 内容 |
|---|---|
| `rinpan` | 森町の林班界（165件）。ジオメトリはGeoJSON文字列 |
| `kosyoban` | 森町の小班（10,301件）＋森林調査簿の属性 |
| `meta` | 出典・座標系・統計 |

`nara_rank` は 2=ナラ類確実（樹種62/63/64）、1=可能性（98/99）、0=該当なし。

---

## 5. API

すべて `http://127.0.0.1:8765/api/...`。認証は無い（ローカル前提）。

| メソッド | パス | 用途 |
|---|---|---|
| GET | `/api/bootstrap` | サイト一覧・ステータス定義・統計・期限 |
| GET | `/api/trees?site=&status=&priority=&rinpan=&q=&bbox=&nara=` | 候補木一覧 |
| GET | `/api/tree/<id>` | 1本の詳細＋写真＋履歴 |
| POST | `/api/tree/<id>` | 更新（JSON。`_who` に担当者名） |
| POST | `/api/tree` | 手入力で追加 |
| DELETE | `/api/tree/<id>` | 削除（`source='ai'` は拒否） |
| POST | `/api/tree/<id>/photo` | 写真アップロード（multipart） |
| POST | `/api/photo/<id>/delete` | 写真削除 |
| GET | `/api/kosyoban?bbox=&nara=` | 小班ポリゴン（GeoJSON） |
| GET | `/api/stats?site=` | 集計 |
| GET | `/api/export.csv` `/api/export.geojson` | 書き出し |

---

## 6. よくある改修

### ステータスを増やす／名前を変える

`tools/db.py` の `STATUS` を編集するだけ。画面の凡例・フィルタ・詳細ボタン・
CSVの日本語表記まで、すべてここを参照している。

### 検出をAIモデルに差し替える

`tools/detect.py` の `detect_candidates(ortho, params, progress)` を差し替える。
戻り値は dict のリストで、最低限これらのキーがあればよい。

```python
{'x': 平面直角XI系の東距, 'y': 北距, 'area_m2': 面積, 'score': 0..1}
```

`ortho` は `tools/ortho.py` の `Ortho` で、`ortho.rgb`（H×W×3 uint8）、
`ortho.alpha`（H×W uint8）、`ortho.sx`（画素の地上寸法m）、
`ortho.pixel_to_xy(col,row)` が使える。

優先度の決め方を変えたいときは同じファイルの `priority_of()` を直す。

### 対象を森町以外の市町村に変える

`tools/build_forest.py` の先頭で

```python
SHINKOKYOKU = '01'   # 渡島
MORI = '15'          # 森町
ZIP_URL = 'https://www.fics.pref.hokkaido.lg.jp/FILE/2024/GIS/01oshima.zip'
```

を変える。市町村コードは `data/_src/oshima/森林調査簿コード.xlsx` の
「市町村コード」シートにある（振興局コード＋市町村コードの組）。
他の振興局は URL の `01oshima` を `02hiyama` などに変える。

`tools/fetch_basemap.py` の `MORI_BBOX` も対象範囲に合わせること。

### 森林データを最新年度に更新する

北海道は毎年オープンデータを更新している。
`ZIP_URL` の年（`FILE/2024/`）を新しい年に変え、
`data/_src/01oshima.zip` を消してから `初期設定.bat` を実行すればよい。
**`forest.db` は作り直されるが、`survey.db` は触られない。**

ただし林班・小班の区画が変わった場合、既存の候補木の `rinpan`/`kosyoban` は
古いままになる。付け直したいときはオルソを再取り込みする。

### タイルの形式を変える

既定は WebP（Pillow が対応していれば）。対応していなければ PNG に落ちる。
`--tile-format png` で強制できる。
サイトごとの拡張子は `sites.tile_ext` に記録され、画面はそれを見てURLを組み立てる。

---

## 7. 検算・動作確認

```bash
# 投影計算が正しいか（引き継ぎ資料の実測値と照合）
py -3 tools/geo.py

# オルソを読めるか・どこに落ちるか（DBには書かない）
py -3 tools/ingest_ortho.py "オルソ投入/siteA_ortho.tif" --dry-run

# サイト一覧
py -3 tools/remove_site.py

# サーバーだけ起動（ブラウザを開かない）
py -3 server.py --no-browser -v
```

---

## 8. 既知の制約

- **森林調査簿は民有林のみ。** 国有林は小班に入らないため、
  そこにある候補は「民有林の小班の外」として優先度が下がる。消してはいない。
- **オルソの絶対位置に水平±3〜5mの誤差。** GCP・RTK未使用。仕様。
  境界確定・図面作成には使えない。画面にも常時表示している。
- **樹高は参考値。** ドローンDSM − 国土地理院DEM。DSMの高さが気圧高度由来のため
  誤差が大きい。サイトごとに下位5%を地面とみなす簡易補正を入れてあるが、目安。
- **ログイン機能は無い。** 単独PC・庁内LAN前提。
  `--host 0.0.0.0` で外に開くときは、信頼できるネットワーク内に限ること。
- **年次比較の画面は未実装。** サイトを年ごとに分けて登録することはできる。
- **点群（LAS）は扱っていない。** Web で見せるなら PotreeConverter か
  py3dtiles を挟む必要がある（元の引き継ぎ資料 3-4 参照）。

---

## 9. 出典とライセンス

| データ | 出典 | 条件 |
|---|---|---|
| 林班・小班・森林調査簿 | 北海道オープンデータ「森林計画関係資料（一般民有林）令和6年 渡島」<br>https://www.fics.pref.hokkaido.lg.jp/FILE/2024/GIS/01oshima.zip | 北海道オープンデータ利用規約（商用・非商用問わず利用可） |
| 標高（DEM10B） | 国土地理院 標高タイル | 国土地理院コンテンツ利用規約 |
| 空中写真・地形図 | 国土地理院タイル<br>https://maps.gsi.go.jp/development/ichiran.html | 同上 |
| 被害状況 | 北海道水産林務部／森町農林課の公表資料 | 出典明記のうえ引用 |
| Leaflet 1.9.4 | https://leafletjs.com | BSD-2-Clause（`app/vendor/leaflet.js` 冒頭に表示） |

森林計画関係資料は「実測によるものではなく、地域森林計画樹立のため間接調査法により
作成」されたもので、**所有権等の諸権利・所有界・面積・木竹の評価を証明するものではない**。

---

## 10. 追加した機能（画面まわり）

### API

| メソッド | パス | 用途 |
|---|---|---|
| GET | `/api/locate?lon=&lat=` | 1点の平面直角座標・林班・小班・樹種・標高を引く |
| GET | `/api/browse?path=` | オルソを選ぶためのフォルダ一覧（ドライブ・フォルダ・画像） |
| POST | `/api/import` | オルソの取り込みを裏で開始（`{path, site, name, date, note, detect, tiles_only, zmax}`） |
| GET | `/api/import/status` | 進み具合・ログ（`{running, rc, percent, phase, lines}`） |
| POST | `/api/import/cancel` | 取り込みを中止 |

取り込みは `server.py` の `ImportJob` が `tools/ingest_ortho.py` を
`subprocess` で起動し、標準出力を1行ずつ拾って画面に返す。
生ログは `data/_import.log` にも残る（失敗時の調査用）。

### 位置の編集

`POST /api/tree/<id>` に `lon`/`lat` を渡すと、サーバー側で
平面直角座標・林班・小班・樹種・代表地番・標高を引き直す
（`locate_point()`）。変更履歴には「位置」としてまとめて1行だけ残す。

### 画面の道具

`app/app.js` の `setMode()` が `add` / `measure` の状態を持つ。
地図のクリックは `onMapClick()` が状態に応じて振り分ける。
現在地は `navigator.geolocation`（HTTPSでなくても `127.0.0.1` は安全な文脈として
扱われるため動く）。座標のパースは `parseCoord()` が
10進・度分秒・平面直角XI系の3通りを受ける。
平面直角→緯度経度は逆変換を持たないので、`/api/locate` を数回叩いて
反復で寄せている（6回で 5cm 以内に収束する）。

### 背景地図のフォールバック

`FallbackTile`（`L.TileLayer` の派生）が、ローカルの
`data/basemap/...` を先に読み、404 なら地理院タイルへ切り替える。
現地（圏外）では取り込み済みの範囲が、事務所ではその外も見える。
コンソールに 404 が出るのは仕様。
